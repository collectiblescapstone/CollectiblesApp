import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.collectibles.app',
  appName: 'collectibles-app',
  webDir: 'public'
};

export default config;
