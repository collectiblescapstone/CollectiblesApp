export type GeoLocation = {
    formatted: string
    lat: number
    lon: number
}

export type GeoFeature = {
    properties: {
        formatted: string
        lat: number
        lon: number
    }
}
