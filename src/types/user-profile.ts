export type SocialsType = {
  icon: React.ElementType;
  handle: string;
};

export type CardType = {
  imageSrc: string;
};

export type tradeCards = {
  card: {
    name: string;
    image_url: string;
  };
};
