export type SocialsType = {
  icon: React.ElementType;
  handle: string;
};

export interface SocialLinksProps {
  instagram?: string;
  twitter?: string;
  facebook?: string;
}

export type CardType = {
  imageSrc: string;
};
